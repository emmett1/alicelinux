# Depends: wlroots json-c cairo pango libglvnd libevdev libinput

name=sway
version=1.8.1
release=1
source="https://github.com/${name}wm/${name}/releases/download/${version}/${name}-${version}.tar.gz"
build_opt="-Dwerror=false"
