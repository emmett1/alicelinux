# Depends: tllist fcft wayland-protocols libxkbcommon

name=foot
version=1.16.2
release=1
source="https://codeberg.org/dnkl/${name}/archive/${version}.tar.gz"

post_build() {
	rm $PKG/usr/share/terminfo/f/foot
	rm $PKG/usr/share/terminfo/f/foot-direct
}
