# Depends: hwdata seatd wayland libdrm libxkbcommon pixman libinput mesa

name=wlroots
version=0.16.2
release=1
source="https://gitlab.freedesktop.org/${name}/${name}/-/releases/${version}/downloads/${name}-${version}.tar.gz"
build_opt="-Dexamples=false"
