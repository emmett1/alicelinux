# Depends: glib libslirp alsa-lib sdl2 pixman

name=qemu
version=8.2.1
release=1
source="https://download.$name.org/$name-$version.tar.xz
	lfs64.patch"
build_type=configure_build
build_opt="--target-list=x86_64-softmmu" # --disable-gtk"
